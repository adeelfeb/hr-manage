<%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>


<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm py-3 px-4">
  <div class="container-fluid">
    <!-- Brand -->
    <%= link_to "HR Manage", root_path, class: "navbar-brand fw-bold text-white" %>

    <!-- Mobile Toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
      aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse justify-content-between" id="navbarContent">
      <!-- Left Menu -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-2">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
          <%= link_to "Documents", documents_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
          <%= link_to "Employees", employees_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
          <%= link_to "Contact Us", contact_path, class: "nav-link text-white" %>
        </li>
      </ul>

      <!-- Right Auth Links -->
      <ul class="navbar-nav mb-2 mb-lg-0 gap-2">
        <% if user_signed_in? %>
          <li class="nav-item d-flex align-items-center text-white px-2">
            👋 Welcome, <%= current_user.email %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-light btn-sm" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-light btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
